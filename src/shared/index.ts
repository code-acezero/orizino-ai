// Shared building blocks consumed by every zone:
// UI kit, Supabase client, auth, currency, language, perf, design tokens.
// Will become packages/ui, packages/supabase, packages/shared, packages/tokens
// in Phase 3.
export {};
